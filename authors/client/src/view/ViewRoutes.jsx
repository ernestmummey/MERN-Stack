import React from 'react';
import Dashboard from './Dashboard'

const ViewRoutes = (props) => {
    return (
        <div>
            <Dashboard path = "/api/authors" />
        </div>
    )
}

export default ViewRoutes; 
